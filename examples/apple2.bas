10 rem This looks cool with cool-retro-term's Apple ][ them!]
20 rem If you have the "play" command installed (on Linux),
30 you could even use that here to play the beep sound. :)
40 clear
50 R = ROWS()
60 prompt chr$(10) + chr$(10) + "]"
70 move 0 R
